import * as React from "react";
//import LabelStudio from './node_modules/@heartexlabs/label-studio/build/static/js/main.js';
//import * as LabelStudio from 'heartexlabs@label-studio';
//import 'heartexlabs@label-studio@latest/build/static/css/main.css';
//import * as LabelStudio from 'label-studio/build/static/js/main.js'
/*
const LabelStudioReact = (props: any) => {
  const labelStudioContainerRef = React.useRef();
  const labelStudioRef = React.useRef();

  React.useEffect(() => {
    if (labelStudioContainerRef.current) {
      labelStudioRef.current = new LabelStudio(
        labelStudioContainerRef.current,
        props
      );
    }
  }, []);

  return (
    <div
      id="label-studio"
      ref={function(el) {
        labelStudioContainerRef.current = el
      }}
    />
  );
}
*/